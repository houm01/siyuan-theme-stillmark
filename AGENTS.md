# Stillmark Theme Rules

## Visual Direction

- 亮色模式以纯白主编辑区为基础，只用低饱和中性色区分侧栏、悬浮层和次级表面。
- 整体保持安静、克制，避免装饰性渐变、拟物终端灯和过度阴影。

## Application Shell

- 应用壳层采用“白色纸面 + 中性工作台”结构；编辑器保持主表面，顶部工具栏、页签、Dock、侧栏和状态栏使用低对比度中性色与发丝线分层，不把编辑器包成悬浮大卡片。
- 红色只承担主题识别、当前文档和活动页签等关键状态；蓝色用于链接、焦点与引导线，绿色只用于成功语义。
- 工具栏、页签、Dock 和侧栏保持紧凑、稳定，不改变原生拖拽、缩放、分栏、折叠或命中区域。

## Search Workspace

- `Cmd + P` 全局搜索使用紧凑工作台层级：搜索输入是主入口，结果列表保持高密度，预览区回归纯净纸面，筛选与快捷键提示降为次级信息。
- 当前结果使用低饱和红色状态，命中词使用同系轻底色；路径和统计保持单行中性文本，不与结果标题争夺注意力。
- 同时支持思源原生上下与左右布局，保留分隔拖拽、筛选、替换、翻页、路径范围、资源搜索和键盘操作，不通过主题强制覆盖用户的布局偏好。

## Code Presentation

- 行内代码使用低饱和暗莓红文字和同系浅底，与蓝色链接区分且保持足够对比度；无可见描边或阴影，并保留紧凑小圆角；不使用 `inline-block`，避免改变正文行高，长内容允许自然换行。
- 代码块使用轻量背景、细边框和清晰焦点态，同时保留语言、复制、菜单、行号和折叠功能的可读性。
- 代码块采用扁平的正文嵌入式面板：小圆角、工具层与正文同底、无常态投影和独立标题条；语言使用无底色的低饱和暗莓红小标签，不使用醒目的红色胶囊。
- 代码块字号、行距、标题栏和内边距保持紧凑，视觉密度接近正文，不形成突兀的大块面板。
- 代码块必须为 Highlight.js 常用语义类提供亮暗双色板；语法高亮使用红、蓝、紫、琥珀等明确区分，代码正文不使用松绿色。

## References

- 块引用和内容引用不使用默认紫色或绿色大段文字；引用标签使用低饱和红色文字和同系浅底色与普通正文区分，不额外显示跳转箭头或下划线。
- 引用样式必须保持行内换行能力，不使用 `inline-block`，并避免影响数据库单元格中的引用文本。
- 文件批注引用使用偏暖的中性色与块引用区分，同时保持相同的紧凑交互骨架。
- 正文中的块引用和内容引用参考 wolai 的紧凑标签：使用浅粉红底色、克制的红色文字与小圆角，不使用跳转箭头、下划线、文字阴影、外投影或完整边框；文件批注引用保持透明与原有箭头、细下划线。
- 文档末尾的“引用页面”使用附录式层级：主题红标题、发丝线边界、紧凑来源栏和带细竖线的次级摘录；它应明显区别于正文，但不堆叠阴影或厚重卡片。
- 行内代码和代码块必须同时覆盖亮色与暗色模式，并保持足够的文字对比度。

## Quotes and Callouts

- 引用块使用透明背景、比正文略轻的正体暖灰文字和 2px 低饱和暗莓红直线，不使用斜体、整块灰底、阴影或粗重圆角边条。
- 提示块使用低对比度中性面板、1px 细边框和紧凑的标题/正文间距，不使用左侧粗色条或外投影。
- Note、Tip、Warning、Important 与 Caution 只通过图标底色和标题色区分，正文保持统一的高对比度中性色。

## Links

- 超链接使用明确的蓝色文字和始终可见的细下划线；悬停时加深文字并增强下划线，不使用胶囊或背景填充；外部网站链接在正文前显示紧凑的站点图标。
- 悬停只调整现有下划线的颜色或强度，不叠加 `border-bottom`、阴影或第二道横线。
- 链接保持行内自然换行，不改变正文行高，并同时覆盖亮色与暗色模式。
- 主题单独安装时使用中性地球图标；源站 `/favicon.ico` 由 Stillmark Workbench 伴生插件使用无 Referer 请求加载并通过主题 CSS 变量增强。不得依赖第三方 favicon 聚合服务，也不得向正文节点写入样式、类名或额外 DOM。

## List Interaction

- 有序、无序列表与上方块相连的圆角引导线只在悬停列表项时显现；它从上方块的左侧接入首项标记，再沿标记轴连接到当前项。
- 引导线保持单一、连续，使用 2px 蓝色实线和圆润转角；不要占用或重绘思源原生 `.li::before`，激活自定义路径时只允许暂时隐藏其灰色边线以避免双线，也不要在标记之间留下断线或重复线段。
- 有序列表的引导轴放在编号左侧并保留可见间距，不得穿过 `1.`、`2.` 等编号；无序列表仍可穿过圆点中心。
- 嵌套列表的引导线从父项标记轴连续向下，绕开父项正文后以连续圆角接入子列表首项标记；多级列表各层不得断开或形成生硬直角。
- 竖线、横线与圆角的拼接必须以 2px 线宽中心轴对齐；相邻伪元素至少保留 1px 重叠，不得只做边缘相接，直线也不得提前穿入圆角内侧形成凸出。
- 连接线不改变列表缩进、折叠、拖拽或任务列表行为。

## Document Tree

- 文档树中的笔记使用克制的主题文档图标，不显示系统彩色 Emoji；仅覆盖 `navigation-file`，保留笔记本根节点图标。
- 图标替换保持原生点击、提示、折叠、拖拽和行高不变；不要占用 `.b3-tooltips` 的伪元素，优先使用元素背景图。
- 文档图标同时提供亮暗配色，并在实际 18px 尺寸下验收清晰度。
- 当前选中的笔记通过 `.b3-list-item--focus` 使用低饱和红色背景持续高亮，只作用于 `navigation-file`，不改变文字、图标、行高或命中区域。
- 文档树引导线沿用列表的 2px 蓝色悬停交互：从展开父项圆角接入当前悬停子项，穿过中间展开分支时保持连续；不改变原生缩进、折叠、拖拽或命中区域。
- 首个子项的圆角横线与叶子末端横线必须落在同一水平像素带，不得出现 1px 错位或断层。

## Bookmarks

- 书签列表保持单行高密度；仅当同组存在同名文档时，为重名条目增加低对比度的父级路径副标题，唯一标题不增高。
- 父级路径优先用于区分重名文档；路径仍相同时再补笔记本范围与文档时间，不改变原生点击、拖拽、菜单或分组行为。重名检测与路径回读由 Stillmark Workbench 伴生插件执行，主题只负责呈现。

## Database Tables

- 数据库表格采用类似 Notion 的扁平内联结构：标题位于视图栏上方，外层不使用圆角卡片、完整边框或阴影，只以细分隔线组织工具栏、表头、数据行和计算区。
- 数据库内页的折叠属性入口只保留透明背景的低对比度图标和文字；不得用整行底色、卡片边框、圆角和阴影强化它，展开后仍沿用扁平发丝线结构。
- 视图栏使用紧凑文字页签和底部激活线，常用工具以低对比度图标常驻；表头与正文使用同一表面，依靠中性图标、500 字重和发丝线区分字段。
- 数据行保持约 34px 的单行扫描密度；行悬停使用极浅中性色，单元格选中和键盘焦点使用 Notion 风格蓝色细描边与轻底色。
- 单选、多选等标签保持原始语义色，使用 4px 小圆角、紧凑内边距且不增加描边或阴影；亮暗模式必须分别校准表面、分隔线和交互色。
- 数据库样式不得改变列宽拖拽、固定列、横向滚动、添加条目、计算、筛选、排序、搜索或视图切换行为。

## Verification

- 修改主题后运行 `git diff --check`，并确认本地思源实际提供的 `theme.css` 与仓库文件一致。
- 发布前运行 `./scripts/build-package.sh`，确认 `package.zip` 只含允许的主题资源、根目录不含 `theme.js`，并用 Bazaar 当前 Stage 检查规则复核。
- 新增或修改 `theme.js` 时同步递增 `theme.json` 版本；调用 `/api/ui/reloadTheme` 后确认 `#themeScript` 的查询版本已更新，避免现有窗口继续使用旧脚本或 CSS 兜底。
- 视觉验收至少覆盖普通正文中的行内代码、长行内代码、带行号代码块、折叠代码块及亮暗模式。
- 浏览器验收只使用隔离无头 Playwright 访问当前思源内核端口的 `/stage/build/desktop/`，不复用或操作用户现有浏览器窗口。
- 先调用 `/api/ui/reloadTheme`，再用现有“主题（静痕）”文档检查代码块与列表；打开全局搜索后先替换遗留查询再截图，避免暴露无关笔记内容。
- 超链接悬停验收在同一隔离页面打开含外链的真实文档，等待 200ms 过渡后分别回读亮暗模式的 idle/hover 样式；必须保持 `text-decoration` 下划线且 `border-bottom` 为零，结束后恢复页面主题属性。
- 暗色验收优先只修改隔离页面的主题属性，不改全局外观配置；结束后恢复页面属性、关闭会话并清理截图和快照。
- 验收文档缺少长行内代码、行号或折叠状态时，只在隔离页面基于真实节点注入临时 DOM 样例，不写回笔记；验收后删除样例。
- 列表引导线必须覆盖“前序同级项自身包含嵌套列表、正文换行、其后仍有同级项”的真实结构；除截图外回读 `.li`、直接子 `.p`、`.protyle-action` 和相关伪元素盒模型，纵向锚点以直接标记行中心为准，禁止用包含子列表的 `.li` 整体 `50%` 代替。
